# OpenRouter (or OpenAI) key — never expose to clients
OPENAI_API_KEY=sk-or-your-key-here
OPENAI_MODEL=openai/gpt-4o

# Shared secret — must match SNAPFLOW_API_KEY in the desktop client .env
SNAPFLOW_API_KEY=change-me-to-a-long-random-string

OPENROUTER_REFERER=http://localhost:5000
