# SnapFlow desktop client — service credentials (not shown in UI)
SNAPFLOW_SERVER_URL=http://localhost:8000
SNAPFLOW_API_KEY=change-me-to-a-long-random-string

# User preference (also editable in Settings)
SNAPFLOW_HOTKEY=ctrl+shift+a
